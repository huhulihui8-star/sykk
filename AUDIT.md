# 项目体检报告 + 修复记录

- 体检时间：2026-09-12
- 仓库：`a-share-sector-radar`（产品名「最强分析师」，A 股 / 美股 / 全球指数信息面展望）
- 接手：WorkBuddy（此前由 Codex 开发）
- 状态：**五个批次全部完成并验证**；剩余项见第三节

---

## 一、修复前基线（实测）

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 生产构建 | `vinext build` | ✅ 通过 |
| 类型检查 | `tsc --noEmit` | ✅ 0 错误 |
| 单元测试 | `node --test --experimental-strip-types test/*.test.ts` | ✅ 16/16（其中 8 个是源码 grep 断言） |
| Lint | `oxlint` | ❌ **40 errors / 0 warnings** |
| 数据链路 | 直接调用 `createMarketSnapshot()` | ⚠️ 单次 **63 个子请求**、11.4s、**267 KB** JSON |

> 63 个子请求已越过 Cloudflare Workers 免费计划的单请求上限（50）。失败方式是静默的——全链路 `Promise.allSettled` + `markStale` 兜底，线上表现为"数据悄悄变旧"而非报错。

---

## 二、已修复项与验证数据

### 批次 1：正确性

| # | 问题 | 修复 | 验证结果 |
| --- | --- | --- | --- |
| P0-3 | A 股 `volumeRatio` 恒为 1（东财 `f8` 抓了但从未写入） | 改用 `f10`（**真·量比**，已用日线反推核对：11156547/7599878 = 1.47 精确吻合）+ `boardMetrics` 直接消费原始字段、按市值加权 | 修复前 A 股 31/31 恒为 1；**修复后唯一值 32 个，区间 0.74–1.5** |
| P0-4 | `momentum5d = change * 0.72` 是假 5 日动量，且与 `returnScore` 重复计权同一信号 | 改用 `f109`（**真 5 日涨跌幅**，已用 K 线反推核对：5 日 12.58% 精确吻合）；美股/指数由日线收盘价反推；取不到时为 `null`，不再用当日涨跌凑数 | 修复后与当日涨跌的比值从恒定 0.72 变为 12.43 / -43.00 / 2.43 / -1.97 等真实值 |
| P0-5 | 无行情时仍输出"上涨 53% / 下跌 47%"这类像概率的数字 | `dataAvailable === false` 时 `nextSessionDirection/UpPct/DownPct` 全部置 `null`，UI 显示"数据不足" | 实测不变量：**有行情却无方向 = 0；无行情却有方向 = 0** |
| P2-6 | `/sectors/:code` 未知代码静默回退到第一个行业（任意乱码 URL 返回 200） | 改为 `notFound()`，并新增 `app/not-found.tsx` | 新增 `/sectors/xxx` → 404 |
| — | 动态取数失败会直接 500 | 新增 `app/error.tsx` 错误边界 | — |
| — | `flatCount` 恒为 0 却参与宽度计算 | `SectorCard` 改用 `up + down`（与详情页一致） | — |
| — | `buildSector` 对同一 `boards.find()` 重复调用 3 次 | 提取为单次 `exactRow` | — |

### 批次 2：请求数与体积

| # | 项目 | 修复前 | 修复后 |
| --- | --- | --- | --- |
| P0-2 | 同一轮里重复抓取的 RSS URL | 5 个（CNBC/WSJ×2/Bloomberg/FT 各抓 2 次，字节数完全一致） | **0** |
| P0-1 | 单次快照出站子请求 | **63**（超 50 上限） | **47**（留出余量） |
| — | 国内指数请求数 | 12 次（6 个指数 × 抓 2 次） | **1 次**（`ulist.np` 批量，一次取回全部 6 个，含真量比与真 5 日涨跌幅） |
| — | `fetchBoards` 请求字段 | 带 `f2/f4/f8/f136`（全程未读取） | 只留真正用到的 11 个字段，响应体更小 |
| P1-2 | `components/ui` 组件 | 60 个，**56 个是死代码**（6025 行 / 180KB） | **4 个**（badge / card / chart / native-select） |
| P1-2 | 产物 CSS | 203 KB | **48 KB（-76%）** |
| P1-4 | OG 图 | `og.png` 1064 KB | `og.jpg` **152 KB（-86%）** |
| — | 客户端 JS+CSS（gzip） | 279 KB | **259 KB** |

> 关于图表：我没有按原计划做懒加载。实测后发现两个原因让收益不划算：图表是首页**首屏主内容**，而 `ssr: false` 会把它推迟到 JS 加载之后（对数据看板是负优化）；`chart` chunk 是 111 KB gzip，是重写成本最高、最容易造成视觉回归的部分。已记入 README 待决事项。

### 批次 3：工程卫生

| 项目 | 修复前 | 修复后 |
| --- | --- | --- |
| Lint | **40 errors**（扫描 83 文件 / 24.6s） | **0 errors / 0 warnings**（扫描 25 文件 / 2.8s） |
| 废弃 API | recharts `<Cell>`（官方 4.0 将移除） | 迁移到 `Bar` 的 `shape` prop + `Rectangle` |
| 单元测试 | 16 个（8 个是源码 grep） | **21 个**（新增 5 个真实**行为测试**，直接调用 `buildSector`） |
| 数据库层 | 两套真值：运行时裸 SQL + 零引用的 `drizzle`（`db/` + `drizzle/` + 2 个依赖） | **单一真值**：`ensureSchema()`，全部非破坏性 DDL；移除 11 个死文件与 2 个依赖 |
| 死 schema | 建了从不读写的 `sector_history`（legacy）、`model_runs` | 移除 |
| 仓库垃圾 | `work/` 23.8 MB（6 份重复构建产物） | **~0.9 MB** |
| 文档 | 无 README、无 `.env.example` | 均已补齐（含请求预算约束与"有意决策"清单） |
| CI | 无 | `.github/workflows/ci.yml`：typecheck + lint + test + build |
| `.gitignore` | 漏了 `tsconfig.tsbuildinfo` | 已加 `*.tsbuildinfo` |
| `tsconfig` | `exclude` 只有 `node_modules` | 补上 `dist` / `.next` / `.vinext` / `.wrangler` / `work` / `outputs` |
| 失效 hook | `hooks/use-mobile.ts`（随 sidebar 一起失效，0 引用） | 移除 |

### 批次 4：载荷、上游降级加固与节假日日历

| # | 项目 | 修复前 | 修复后 |
| --- | --- | --- | --- |
| — | 首页 props（JSON.stringify 口径） | 203 KB | 79 KB |
| — | **首页真实线上载荷**（HTTP 实测） | **267.4 KB** | **230.5 KB（-13.8%）** |
| — | 其中 RSC flight 载荷 | 114.8 KB | **77.9 KB（-32%）** |
| — | Yahoo 冷启动请求 | 每次刷新固定 23 个 | 冷启动 23 → **热刷新 0**（休市窗口 6 小时内复用） |
| — | 东财板块请求 | 每次刷新固定 5 个 | 冷启动 5 → **热刷新 0** |
| — | 空快照占用缓存周期 | 全空结果被钉住整个 TTL（最长 15 分钟页面无数据） | **60 秒后立刻重试** |
| — | 节假日 | 只判断周末，长假期间误判为交易日 | **接入 CN（上交所公告）+ US（NYSE 官方）日历**，含提前收盘日 |

**首页载荷：先纠正我自己的口径错误。**

我最初用 `JSON.stringify` 测出"每个行业的 evidence 合计 228 KB"，并据此认为首页载荷有 228 KB 是重复内容。**这个判断是错的。** 做了受控 A/B（同一份快照，切换投影开/关）之后的实测：

```
                  投影后        未投影       差值
HTML 总长         250.3 KB     267.4 KB    17.1 KB
其中 flight 载荷   97.7 KB     114.8 KB    17.1 KB
其中 HTML 标记    152.6 KB     152.6 KB     0 KB
```

原因：React 的 RSC 序列化**按引用去重**——同一篇新闻在 60 个行业的 `evidence` 里只编码一次，其余是引用。所以 `JSON.stringify` 的口径（203 KB → 79 KB）严重高估了真实线上成本，**单单剥离证据链的真实收益只有 17.1 KB / 6.4%**。

- **保留**这项改动（17 KB 是真实收益，且 `DashboardSector` 类型从类型层面保证首页不会读到证据链）；
- 但文档里**不再声称 61% 的载荷下降**。

**顺带发现的更大一块**：flight 载荷占整页 43%，而看板每个区域只用前 6 条事件——原来却把 74 条全发给浏览器。投影现在按区域各留 10 条（6 条 + 4 条余量，`DASHBOARD_EVENTS_PER_REGION` 由看板与投影共用，避免两边数字漂移），并丢掉无关联行业的事件。两项合计的最终实测：

```
                   基线        最终        变化
HTML 总长         267.4 KB    230.5 KB    -36.9 KB (-13.8%)
其中 flight 载荷   114.8 KB     77.9 KB    -36.9 KB (-32%)
其中 HTML 标记    152.6 KB    152.6 KB      0 KB
首页收到的事件数        74          30
```

**Yahoo 降级加固**：本机实测 Yahoo 直接返回 `403 + HTML 错误页（不是 JSON）`。**后续（第六轮）查明这不是限流，而是 Yahoo 自 2021-11-01 起对中国大陆的官方地区封锁**——与请求频率无关（换 UA、换路径、连首页都是同一个 3125 字节 403 页），因此缓存治不了它，详见第四节更正。缓存层本身仍有价值（保护东财 / CNBC / WSJ 的抖动）。缓存策略按「**该市场自己是否在交易时段**」决定复用窗口（开市 3 分钟 / 休市 6 小时 / 失败回退 24 小时内副本），存储为 isolate 内存 → Cache API 两层。用合成 Yahoo 响应做端到端验证：

```
第 1 次构建（冷缓存）: Yahoo 请求 23 次, 东财 clist 5 次
第 2 次构建（同进程）: Yahoo 请求  0 次, 东财 clist 0 次   ← 指标逐项一致
周六休市 → delayLevel 正确为 LAST_CLOSE
```

回退路径（网络失败时返回过期副本并标 `stale`）用注入式测试确定性验证，不必真的等 6 小时。

**空快照重试**：验收过程中真实踩到一次——冷启动时那一轮网络整体失败，产出全空快照，被钉住整个 15 分钟 TTL，页面一直显示"暂无数据"。现在"一个行情都没取到"只缓存 60 秒就重试；Yahoo 单边降级（部分有数据）仍走正常 TTL，避免变成每分钟重试反而更容易被上游拒绝。

**节假日日历**：CN 按上交所《2026 年部分节假日休市安排的通知》（上证公告〔2025〕45 号）**逐条录入休市区间**；US 按 NYSE 官方日历录入 2026 + 2027，并含提前收盘日（13:00 ET）。新增测试会**用公告原文标注的星期反向校验**录入，抄错日期会被测出来。

---

### 批次 5：图表换自绘 SVG（落地）+ 发布上线

先做了一版对比预览（`work/preview/`，含实时对拍面板），确认视觉与交互一致后再落地。

**体积**（增量口径 = 含图表代码 − 纯 React 底座，minify + gzip）：

| | 增量 gzip |
| --- | --- |
| recharts 4 张图 | 111.3 KB |
| 自绘 SVG 4 张图 | **3.8 KB** |

产物核验：`dist/` 全部 js 里 `recharts` 出现 **0 次**；客户端 js+css 现在 **154 KB gzip**。

**几何一致性**（与 recharts 真实渲染逐项对拍，同一份数据、同尺寸）：

| 图表 | 结果 |
| --- | --- |
| 行业涨跌幅横柱 | 刻度、柱子包围盒 **0px** |
| 量能 + 宽度双轴 | 刻度、柱子、曲线 **0px** |
| 信息面环形 | 路径逐点偏差 **6e-5px**（= recharts 4 位小数的舍入精度） |
| 60 日面积 | 面积/折线 **0px**；标签集一致 |

**顺带拿到的两个收益**：

1. **图表服务端直出**。recharts 时代首屏 HTML 里没有图形（要等 JS），现在是真实 SVG（首页 HTML 含 52 个 `<path>`）。首屏就能看到图。
2. **入场动画不依赖 hydration**。用 CSS 动画（柱子 400ms 从 0 轴生长、面积/折线 1500ms 从左揭示、环形 1500ms 延迟 400ms），时长与缓动取自 recharts 默认值，`prefers-reduced-motion` 可直接关掉。用 JS 逐帧会先显示成图再回跳重播，所以没用。

**过程中修掉的两个真实 bug**（都是先写错、被验证抓住的）：

- 环形图第一版按官方文档理解成 `startAngle:90 / endAngle:-270`，画出来第一段位置差约 36°。实测 recharts v3 的默认是 **`0 / 360`**，且每段跨度 = `(360 − 段数×间隙) × 占比`。
- 密集日期轴的标签稀疏函数**算了"末标签左移贴边"但只返回布尔数组**，渲染仍用原坐标 → 末标签溢出被裁。已改为返回位移后坐标，并补了"所有显示标签必须落在绘图区内"的单测。

**发布**：<https://a-share-analyst-dashboard.app.workbuddy.host/>（新增 `npm run serve` 作为单端口启动命令，监听 `$PORT`、绑 `0.0.0.0`）。线上实测 HTTP 200 / 1.2s，A 股 31 个行业全部有行情，`/api/market` 与 `/og.jpg` 均正常；`layout.tsx` 的 `metadataBase` 原指向旧域名，已改为可由 `NEXT_PUBLIC_SITE_URL` 覆盖并回落到当前地址。

---

### 批次 6：Yahoo 换源 + 新闻源扩容（用户反馈「样本太少、参考性不大」）

**换源（用户确认后执行）**：美股 11 行业 ETF 改用东财 `107.*`、12 个海外指数改用 `100.*`（罗素2000 用 `107.IWM` ETF 代理并披露），
全部合并进一次 `ulist.np` 批量请求。**Yahoo 的 23 个请求全部移除**，Google News 的 9 个死请求也一并删掉。

**新闻源扩容**：移除 Bloomberg / FT（不可达），新增 CNBC ×4、WSJ Opinion、MarketWatch ×3、Nasdaq ×2、Fortune、Seeking Alpha，
以及中文侧的新浪 7x24、华尔街见闻全球快讯、东财机构研报。每个 URL 都实测过可达性。

**修的是「参考性」，不只是「条数」** —— 这是本轮最重要的发现，也纠正了对用户反馈的初始理解：

| 层 | 改前 | 改后 |
| --- | --- | --- |
| 样本条数（A 股信息池） | 28 | **146** |
| 国内池来源数 | ~17 | **60** |
| **携带方向的样本（正+负）** | **12** | **36** |
| **判为「中性」的比例** | **85%** | **57%** |
| **A 股信息分不等于 50 的行业** | 几乎为 0（大面积恒等于 50） | **29 / 31**（范围 43.5~56.5） |

主因不是条数，而是**旧情绪词典让 85% 的事件落到中性 → 中性的新闻对分数没有贡献 → 信息分恒等于 50**。
只扩源不修词典，样本再多也没用。同时修掉一个方向性 bug：旧实现用子串匹配，`rise` 会命中 `surprise`、`miss` 会命中 `dismissed`。

**补上的结构约束**（让「样本少」不再伪装成「有结论」）：单来源打分配额（`diversifyBySource`）、
证据置信度（`evidenceConfidence`，把信息分向中性收缩）、
**某池为空时按比例还给另一池**（`weightedChinaEvidenceScore`，修掉「海外 0 条就让 60% 权重变成常数 50」的结构性稀释）、
展示列表单来源削峰（`capPerSource`）。

**顺带修掉一个一直存在的失真**：原来美股指数的「涨跌家数」是用 ETF 自身涨跌方向伪造的（`change>0 ? 1 : 0`），
等于把 `returnScore` 重复计入 `breadthScore`。现在指数与 ETF 的广度项按中性处理，不再伪造。

**验证**：`tsc` ✅ / `oxlint` 0 / 单测 **44** 个全绿 / 构建 ✅ / 已重新发布。
线上实测：A 股 31/31、**美股 11/11**、**指数 18/18** 全部有行情，`dataStatus` 从 `STALE` 变为 `LIVE_PROXY`，
快照构建 **3.5s**（改前要等 Google News 超时十几秒），`modelVersion` 升到 **v9**。

完整实测清单、不可达来源与仍缺的部分见 [`NEWS-SOURCES.md`](./NEWS-SOURCES.md)。

---

### 批次 7：补齐海外指数的历史行情（用户反馈「这两个模块不显示」）

详情页的「60 日走势」与「量能与市场宽度」都靠 `fetchHistoryFromProviders`。上一轮只修好了 A 股行业、
国内指数、美股行业与美股指数；**海外指数 6 个（日经/KOSPI/台湾加权/DAX/富时100/恒生）仍然是空白**。

**思路转向**：不再找「谁家有全球指数数据」，而是先搞清**同花顺自己的代码体系**。
同花顺的行情节点用「市场_代码」命名，实测确认三段：`bk_` = A 股行业板块、`hk_` = 港股指数、
**`88_` = 全球指数**。先穷举 `88_` 下的名称变体，再与实时行情逐位比对：

| 指数 | 同花顺代码 | 末根收盘 | 与实时行情 |
| --- | --- | --- | --- |
| 日经 225 | `88_N225` | 64011.34 | 一致 |
| 韩国综合 | `88_KS11` | 6909.91 | 一致 |
| 台湾加权 | `88_TWII` | 46184.85 | 一致 |
| 德国 DAX | `88_GDAXI` | 25568.56 | 一致 |
| 英国富时 100 | `88_FTSE` | 10650.44 | 一致 |
| 恒生指数 | `hk_HSI` | 24805.63 | 一致 |

这 6 个给的是**指数本身**的日线，不是 ETF 代理，点位与页面顶部实时行情同源同口径。

**过程中抓到两个真问题（都不是"数据源没有"，而是"读法不对"）**：

1. **同花顺 `last.js` 的分片更新进度不一致。** `hk_HSI` 的 `/00/`、`/01/` 停在 2026-04-21，
   只有 `/02/` 到最新交易日；而**线上沙箱**还观察到 `/02/` 拿到旧窗口——同一份代码先返回 06-18~09-11，
   十几分钟后再取就变成 01-20~04-21。这是**边缘节点同步不同步**，不是限流也不是权限。
   修法：同时取 `/01/` 与 `/02/`，交给 `freshestSeries` 选末条日期更新的那份，不赌单一分片。
   （`bk_*` 三个分片实测一致，仍只取一个，不翻倍请求。）
2. **纵轴刻度会裁字，看起来像"数值算错"。** 指数点位是 8 个字符（`26029.46`），
   固定 48px 的纵轴把首位裁掉、页面上显示 `6029.46`。我一开始把它读成"数据错了"，查了 API 才发现数据是对的。
   修法：`axisWidthFor()` 按最宽标签动态留宽（纯函数估算，保证 SSR 与客户端一致）。

**另一处诚实性处理**：同花顺对 `88_N225` / `88_KS11` / `88_TWII` / `88_GDAXI` / `88_FTSE`
的成交量字段用**常量 `1` 占位**（`88_SPX`、`hk_HSI` 是真实量）。照原样透传会让量能图画出一排等高柱、
纵轴 0~1 毫无信息量却看着像有数据。`normalizeThsIndexVolume` 把它当成「无成交量」置 0，
UI 改成如实显示"该原始指数数据源不提供可核验成交量"。

**欧元50 已按用户要求移除**（不是"保留一个永远数据不足的页面"）：东财只有它的实时行情、没有 K 线；
同花顺全球指数段（试遍 1–99 市场号 × 40 余种代码拼法）、腾讯、新浪、WSCN、stooq、CNBC 全部拿不到历史日线。
指数定义与 `100.*` secid 映射同时删除，并加了单测锁住"不再引入"（`欧元50 已移除，且没有留下孤立的 secid 映射`）。

罗素2000 仍用 `IWM` ETF 代理（东财 `m:100` 全表 63 项里确实没有该指数）。

**验证**：`tsc` ✅ / `oxlint` 0 / 单测 **54** 个全绿 / 构建 ✅ / 已重新发布。
线上实测：**走势图与量能模块 11/11 个海外指数页全部渲染**，6 个新指数的末值全部 = 实时值、`historyMeta.status = FRESH`。

---

## 三、未修复 / 待决策

1. ~~**recharts 111 KB(gzip) 占客户端 JS 的 40%**~~ **已在批次 5 完成**。唯一保留的差异：环形图的入场动画是缩放淡入，不是 recharts 的"扇形扫开"。

2. ~~**美股 11 行业 + 12 个海外指数全部不可用（Yahoo 地区封锁）**~~ **已在批次 6 完成换源。**
   实测结论：Yahoo 自 2021-11-01 起对大陆返回 403，**不可能靠缓存或重试修复**。已改用它家已可达的源：

   | 目标 | 现状 | 替代方案 | 实测 |
   | --- | --- | --- | --- |
   | 美股 11 行业 ETF | ✅ 11/11 | 东财 `107.XLK…XLC` | 含价、涨跌幅、5 日涨跌幅、量比、成交量 |
   | 美股 4 指数 + 恒生/日经/DAX/富时/KOSPI/台湾加权 | ✅ 11/11 | 东财 `100.*` | 含 5 日涨跌幅（欧元50 已于批次 7 移除，见下） |
   | 罗素2000 | ⚠️ ETF 代理 | 东财无该指数，改用 `107.IWM` | 已在 `proxyDisclosure` 披露 |
   | 涨跌家数（`f104/f105`） | ❌ 仍缺 | 东财美股无此字段 | 指数/ETF 的广度项已改为中性，不再用 ETF 自身涨跌伪造 |
   | 60 日 K 线 | ❌ 仍缺 | 东财 `push2his` 各分片主机 socket 直接断、`push2delay` 的 kline 恒为 0 | 与 Yahoo 无关；见下一条 |

3. ~~**A 股板块的日线历史仍拿不到（详情页 60 日走势图显示「数据不足」）**~~ **已在批次 7 解决。**
   结论修正：东财 K 线在本环境**彻底不可用**（`push2his` 各分片主机 socket 直接断；`push2delay` 的 kline
   6 种参数组合一律 `klines=0`；腾讯/新浪都不支持东财板块代码），所以换了一条路：

   | 标的 | 来源 |
   | --- | --- |
   | A 股 31 个行业 | 同花顺行业板块日线（90 个二级板块按申万一级等权聚合） |
   | 国内指数 6 个 | 新浪 A 股指数日线（腾讯作冗余） |
   | 美股 11 个行业 | 新浪美股日线（ETF 代码） |
   | 美股 5 个指数（含罗素2000 用 IWM 代理） | 新浪美股日线（标普500 = `.INX`） |
   | 海外指数 6 个（日经/KOSPI/台湾加权/DAX/富时100/恒生） | **同花顺全球指数日线**（`88_*` / `hk_*`，指数本身而非 ETF） |

   行业序列由「等权平均涨跌幅复利复合」得到，**宽度取当日上涨子板块占比（真实宽度）**，
   所以「量能与市场宽度」模块才真正有内容。
   线上实测：**14 个指数页里 13 个**两个模块都渲染（唯一的例外是已移除的欧元50）。

   **重要环境差异**：**线上沙箱连不上腾讯、但能连新浪**——只配腾讯的指数页在线上全部失败，
   而本机都能成功。判断数据源可用性时本机结论不能直接套到线上。

4. **新闻源没有缓存**
   行情侧有 `readCachedQuote`，新闻侧没有，每轮构建都要实打实打一遍十几个上游。
   若要继续加源，建议先补这一层。

5. **未覆盖市场的节假日**
   节假日日历只覆盖 CN / US。港股、日股、韩股、台股、德股、英股的休市日未建模，这几家指数目前只按周末判断。`test/market.test.ts` 里有一条测试专门锁定这个已知缺口，补齐后该测试应改为断言正确休市。

6. **A 股 2027 年休市安排未公布**
   上交所通常在当年 12 月发布次年安排。届时补录 `CN_MARKET_CLOSURES` 并同步补星期校验测试。

7. **`marketPhase('INDEX')` 只按 CN/US 聚合**
   指数页的"盘中观察 / 下一交易日展望"只参考中美两个市场；港日韩欧台市场即便在交易时段也不会被单独计入。改这个会改变指数页现有语义，需要产品确认。

8. **`outputs/` 22.8 MB 未清理**
   12 个 `site-*.tgz`（v9 → v19）看起来是历史发布包。删除不可逆且本机 git 二进制不可用，**所以我没有动，等你确认**。

9. **`.oxlintrc.json` 中两处规则覆盖需要你确认是否认可**
   - `test/**` 关闭 `no-floating-promises`：`node:test` 的 `test()` 返回 Promise，属误报。
   - `app|components/**` 关闭 `nextjs/no-html-link-for-pages`：项目**有意**不用 `next/link`（`test/market.test.ts` 还断言不许出现），理由已写进 README。

---

## 四、对初版报告的更正

体检初版有三处判断不准，加上后续自己修正的一处，一并更正：

1. **未使用的 UI 组件：初版报 44 个，实际是 56 个。**
   初版脚本把 `components/ui/` 内部的互相引用也算成了"被使用"，导致 `button`、`input`、`label`、`separator` 等被误判为在用。修正统计口径后，真正被业务代码引用的只有 `badge / card / chart / native-select` 四个。

2. **CSS 浪费被高估。**
   初版说 208 KB 里约 72%（≈150 KB）对应未使用的 class，暗示这是传输浪费。实测 gzip 后 CSS 只有 30 KB，未使用部分约 21 KB。真正的收益在**解压后的解析量**：清理后 CSS 从 203 KB 降到 48 KB。

3. **首页载荷的"重复证据链 228 KB"是错的，这是最严重的一处误判。**
   初版用 `JSON.stringify` 测出 60 个行业的 `evidence` 合计 228 KB，并断言"同一篇新闻被重复序列化"。实测 React 的 RSC 序列化**按引用去重**，同一对象只编码一次。受控 A/B 显示单剥离证据链的真实收益是 **17.1 KB（6.4%）**，不是 228 KB。
   教训：**载荷类结论必须测 HTTP 层的真实字节，不能用 `JSON.stringify` 代替**——RSC/flight 是带引用表的格式，字符串化口径会系统性高估重复内容。最终真正的大头是**事件总数**（74 → 30），而不是我以为的证据链。

4. **Yahoo 的 403 不是"限流"，是官方地区封锁（这条是我自己后来推翻的）。**
   批次 4 我把 Yahoo 的 403 归因为"这个 IP 请求太多被限流"，并据此加了缓存层、还写了"限流加固"。第六轮实测推翻了它：Yahoo 的 403 页面（`lang="zh"`、3125 字节、`server=ATS`）正文写着
   *“As of November 1st, 2021 Yahoo's suite of services will no longer be accessible from mainland China.”*
   且 `query1` / `query2` / 首页 / 根路径返回**完全相同**的页面，换 UA、不带 UA 也一样 —— 与请求频率无关。
   教训：**把失败模式归因成"限流"之前，要先读响应正文**。如果我当时多看一眼那 3125 字节的 HTML，就不会花一轮去做治不了病的缓存加固（缓存本身对其他上游仍有价值，所以保留）。
   同时更正：项目里**没有真的 MCP**，`query1.finance.yahoo.com` 是被直接 `fetch` 的，"stock-mcp（yfinance）兼容链路"只是展示用的名称。

---

## 五、环境备注（供后续接手参考）

- 本机 **Bash 的 PATH 已损坏**（`dirname`/`ls`/`head` 均 `command not found`），**PowerShell 不回显 stdout**。
  可靠做法是用绝对路径调 node：
  `/c/Users/huhul/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`
  例如 `node --experimental-strip-types <script>` 可直接 `import` `lib/market.ts` 做真实链路验证。
- **git 二进制不可用**（只有 `.git` 目录，找不到 `git.exe`）。因此本次所有删除操作都先做了备份：
  - `work/removed-ui-components/` — 被移除的 56 个 UI 组件
  - `work/removed-drizzle-layer/` — 被移除的 `db/` + `drizzle/` + `drizzle.config.ts`
  - `work/removed-hooks/` — 被移除的 `use-mobile.ts`
  想恢复任意文件，直接拷回原位即可；`button`、`input` 等也可以用 `npx shadcn@latest add <name>` 重新生成。
- 复现本次验证的命令：
  ```bash
  node node_modules/typescript/bin/tsc --noEmit
  node --test --experimental-strip-types test/market.test.ts test/mcp-auth.test.ts
  node node_modules/oxlint/bin/oxlint
  node node_modules/vinext/dist/cli.js build
  ```
