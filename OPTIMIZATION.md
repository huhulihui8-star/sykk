# 最强分析师优化验收（2026-09-16）

本次仅本地修改、提交和验收，未推送远程或重新发布。保留上涨／下跌二选一，方向阈值及模型 v10 权重未调整；缺行情仍显示数据不足。

## 1. 版本基线

原有工程清理、SVG 图表和数据链路分三批完整归档：0b8484e、17c82ff、b16300f。基线标签 analyst-baseline-2026-09-16。历史发布包及备份保留；环境密钥、运行状态、生成文件和本地数据库排除。.env.example 已解除忽略；tsconfig.tsbuildinfo 已移出跟踪。

## 2. 运行与线上验收

原有 54 项测试和新增 13 项行为测试均通过；类型检查、Lint、生产构建通过。本地三市场首页、801010、US-XLK、IDX-JP-NIKKEI 详情及评估页返回 200；未知详情／API 404；MCP 和留档 POST 无密钥 401。浏览器验证三市场切换、指数总览盘中、日本自身收盘阶段与真实历史 SVG。

现有 WorkBuddy 首页与市场 API 本轮均返回 200，模型 provider-split-direction-share-v10，覆盖 31／11／17，API 更新于 2026-09-16T07:05:52.882Z。该结果是本轮探测，不以旧发布记录代替验收；线上仍运行原发布版本。Sites 项目查询 NOT_FOUND，不重新创建项目。

Windows workerd 本地模拟启动出现访问异常，已使用 Node 生产预览加持久 SQLite 完成本地验收。npm 不在当前 shell PATH，检查直接调用本地 Node 工具，结果通过。

## 3. 缓存与新鲜度

新闻按规范化来源 URL 缓存 5 分钟，失败最多复用原始时间起 30 分钟；冷失败退避 1–5 分钟。同轮启动所有独立抓取，共享重复 RSS，最多 6 路并发，行情任务优先入队。Cache API 每轮最多 50 次调用，内存缓存保底。新闻缓存不提供虚构内容；移除了原有三条静态新闻回退。

旧行业补齐要求原始行情不足 24 小时，保留时间并标记降级。完整旧快照过期也清除方向。历史新鲜度比较自身市场最近已收盘交易日期，抓取成功不等于历史最新。诊断增量披露请求次数、耗时、有效新闻数、覆盖率、来源时间、缓存及失败原因。

实测冷刷新 38 次请求、约 8.01 秒，覆盖 31／11／17，单个 BABA RSS 超时；热刷新 29 个新闻源命中、0 次上游请求、约 156 毫秒，覆盖保持完整。先前首页冷请求约 6.76 秒；网络波动和测量口径不同，不能认定冷启动无回归。热刷新显著减少重复请求；冷请求超时仍需持续观测。

## 4. 指数交易阶段

新增六市场官方休市日，按自身市场时区／午休／半日市计算；总览任一市场开市即盘中。日本收盘修正到 15:30。官方来源、覆盖年份及特殊日限制见 MARKET-CALENDAR.md；未知年份明确披露且不猜下一交易日。

## 5. 分析效果评估

新增不可覆盖预测表及结果表，迁移全部 CREATE IF NOT EXISTS。收盘后第 20–50 分钟为固定留档窗口；五分钟轮询，各标的当天首次成功记录后不覆盖，错过不补写。保存分数、方向、全量评分证据和证据时间、快照时间、模型版本，独立于页面缓存。行情降级、历史未含当日收盘、未来／无效证据时间和未知日历均跳过。

下一交易日收盘相对前收盘评估；行业代理必须同源、相邻交易日期，并在同一序列计算收益以消除窗口复基影响。零涨跌单列；基准为沿用当日实际涨跌方向；披露命中率、方向分布、有效样本数及共同基准样本命中率。不宣称真实概率，不调权。

运行 npm run preview:persistent（构建后）可启动持久化本地预览及任务，数据位于忽略的 .data/analyst.sqlite。本地任务已运行；本轮部分标的因历史／证据时点条件不满足而跳过，空评估如实显示，不制造历史样本。Node 托管可沿用独立 npm run forecast:watch；发布时需持久卷或 D1 和 FORECAST_JOB_KEY，当前线上未启用新任务。

公开 GET /api/forecasts 为聚合评估，/evaluation 为页面。POST /api/forecasts/capture 需要独立任务密钥，code 加 mode=capture 或 evaluate；无数据库返回 503。调度器 --once --dry-run 仅展示计划，不写库。

## 剩余限制

尚未累积足够真实预测结果；休市日覆盖需逐年更新；临时停市及韩国特殊开盘、德国年底特殊时段仍需核实；Windows Worker 模拟需修复本机运行时。冷启动性能尚需稳定网络下对照测量。线上新评估功能和持续任务需后续发布才会生效。
